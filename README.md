# Repository scaffolding

This repository contains tooling to gather repo facts and a minimal Node.js scaffold.
Run the gatherer:
  powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\gather_repo_facts_fixed.ps1
